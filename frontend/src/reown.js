// Placeholder integration helper for Reown AppKit.
// Replace this with the official Reown AppKit SDK usage per their docs.

export function initReown(apiKey){
  // Example: window.Reown = new ReownSDK({ apiKey })
  console.warn('Reown AppKit placeholder — install and init official SDK with REOWN_API_KEY')
}
